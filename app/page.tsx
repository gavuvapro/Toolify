import Link from "next/link";
import { ArrowRight, GitCompare, Heart, Layers, Search, Sparkles } from "lucide-react";
import { SearchExplorer } from "@/components/search-explorer";
import { Button } from "@/components/ui/button";
import { Card } from "@/components/ui/card";
import { ToolCard } from "@/components/tool-card";
import { categories } from "@/lib/catalog";
import { tools } from "@/lib/data/tools";
import { stacks } from "@/lib/data/stacks";
import { appName } from "@/lib/utils";
export default function HomePage() { const featured=tools.filter(t=>t.featured).slice(0,6); return <><section className="relative overflow-hidden border-b"><div className="absolute inset-0 gradient-grid opacity-60"/><div className="relative mx-auto max-w-7xl px-4 py-20 md:py-28"><div className="max-w-4xl"><p className="mb-4 inline-flex rounded-full border bg-card px-3 py-1 text-sm text-muted-foreground"><Sparkles className="mr-2 h-4 w-4 text-primary"/>Open-source Product Hunt for everyday tools</p><h1 className="text-4xl font-black tracking-tight md:text-7xl">Discover, compare, and stack the best tools for your work and life.</h1><p className="mt-6 max-w-2xl text-lg text-muted-foreground">{appName} helps developers, designers, students, creators, founders, and teams choose tools by category, profession, pricing, platform, AI features, and real workflow fit.</p><div className="mt-8 flex flex-wrap gap-3"><Button asChild size="lg"><a href="#discover">Explore tools <ArrowRight className="h-4 w-4"/></a></Button><Button asChild variant="outline" size="lg"><Link href="/stacks">View profession stacks</Link></Button></div></div><div className="mt-12 grid gap-4 md:grid-cols-4">{[[Search,"Instant fuzzy search"],[GitCompare,"Side-by-side compare"],[Layers,"Workflow stacks"],[Heart,"Local favorites"]].map(([Icon,label])=><Card key={label as string} className="bg-background/70"><Icon className="h-5 w-5 text-primary"/><p className="mt-3 font-semibold">{label as string}</p></Card>)}</div></div></section><section className="mx-auto max-w-7xl px-4 py-14"><div className="mb-6 flex items-end justify-between"><div><h2 className="text-3xl font-bold">Featured tools</h2><p className="text-muted-foreground">Popular tools across AI, productivity, design, development, marketing, and more.</p></div><Link href="/categories" className="text-sm font-medium text-primary">All categories</Link></div><div className="grid gap-4 md:grid-cols-2 lg:grid-cols-3">{featured.map(t=><ToolCard key={t.slug} tool={t}/>)}</div></section><section className="border-y bg-muted/40"><div className="mx-auto max-w-7xl px-4 py-14"><h2 className="text-3xl font-bold">Categories</h2><div className="mt-6 flex flex-wrap gap-2">{categories.map(c=><Link key={c} href={`/categories/${c.toLowerCase().replace(/[^a-z0-9]+/g,"-")}`} className="rounded-full border bg-card px-4 py-2 text-sm hover:border-primary hover:text-primary">{c}</Link>)}</div></div></section><section className="mx-auto max-w-7xl px-4 py-14"><h2 className="text-3xl font-bold">Profession stacks</h2><div className="mt-6 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{stacks.slice(0,6).map(s=><Card key={s.slug}><p className="text-sm text-primary">{s.profession}</p><h3 className="mt-1 text-xl font-bold">{s.name}</h3><p className="mt-2 text-sm text-muted-foreground">{s.description}</p><Button asChild variant="outline" className="mt-5"><Link href={`/stacks/${s.slug}`}>Open stack</Link></Button></Card>)}</div></section><section id="discover" className="mx-auto max-w-7xl px-4 py-14"><h2 className="text-3xl font-bold">Explore the library</h2><p className="mb-6 text-muted-foreground">Filter by profession, budget, category, free plan, platform, team support, AI capabilities, and tags.</p><SearchExplorer tools={tools}/></section><section className="mx-auto max-w-7xl px-4 py-14"><Card className="bg-primary text-primary-foreground"><h2 className="text-3xl font-bold">Help keep {appName} free and open source.</h2><p className="mt-2 opacity-90">Support curation, hosting, maintenance, documentation, and community moderation.</p><Button asChild variant="secondary" className="mt-6"><Link href="/donate">Donate</Link></Button></Card></section></> }
